@model IEnumerable<MvcAppMain.Models.Benh>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenBenh)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenBenh)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Benh }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Benh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Benh })
        </td>
    </tr>
}

</table>
